#include <stdio.h>
#include "ed2.h"

int main() {
    int intentadas[26] = {0};
    
    // Mostrar letras restantes después de los intentos
    mostrarLetrasRestantes(intentadas);
    
    return 0;
}